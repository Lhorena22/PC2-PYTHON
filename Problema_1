# Inicializar una lista para almacenar los números que cumplen con las condiciones
numeros_divisibles = []

# Iterar a través del rango de 1500 a 2700 (ambos incluidos)
for numero in range(1500, 2701):
    # Verificar si el número es divisible por 7 y múltiplo de 5
    if numero % 7 == 0 and numero % 5 == 0:
        # Agregar el número a la lista
        numeros_divisibles.append(numero)

# Imprimir la lista de números que cumplen con las condiciones
print("Números divisibles por 7 y múltiplos de 5 en el rango de 1500 a 2700:")
print(numeros_divisibles)